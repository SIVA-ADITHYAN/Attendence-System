// Central API configuration
// Change this URL when switching between local dev and production

const API_BASE_URL = 'https://attendx-s9ju.onrender.com' || 'http://localhost:8080';

export default API_BASE_URL;
